// Stub – full OT logic wired in next phase
function handle(_ws, _sessionId, _user, _payload) {
  // TODO: apply op, transform against concurrent ops, broadcast, checkpoint
}

module.exports = { handle };
